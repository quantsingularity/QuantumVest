# ----------------------------------------------------------------
# QuantumVest Environment Variables Example
# ----------------------------------------------------------------

# This file contains all environment variables used by the QuantumVest project.
# Copy this file to .env and fill in your desired values.

# ----------------------------------------------------------------
# 1. Core Application Configuration (Used by code/backend/config.py)
# ----------------------------------------------------------------

# Flask Secret Key. MUST be a strong, random string.
SECRET_KEY=dev-secret-key-change-in-production

# JWT Secret Key. If not set, defaults to SECRET_KEY.
# JWT_SECRET_KEY=

# Database Connection URL
# Default: postgresql://quantumvest:password@localhost/quantumvest
# For development (if PostgreSQL is not set up): sqlite:///quantumvest_dev.db
DATABASE_URL=postgresql://quantumvest:password@localhost/quantumvest

# External API Keys
ALPHA_VANTAGE_API_KEY=your_alpha_vantage_api_key
COINAPI_KEY=your_coinapi_key
YAHOO_FINANCE_API_KEY=your_yahoo_finance_api_key

# Blockchain settings
# Default: https://mainnet.infura.io/v3/your-project-id
WEB3_PROVIDER_URL=https://mainnet.infura.io/v3/your-project-id
ETHEREUM_NETWORK=mainnet

# Redis settings (for caching and sessions)
# Default: redis://localhost:6379/0
REDIS_URL=redis://localhost:6379/0

# Celery settings (for background tasks)
# CELERY_BROKER_URL=redis://localhost:6379/0
# CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Email settings
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your_email_username
MAIL_PASSWORD=your_email_password
MAIL_DEFAULT_SENDER=your_default_sender_email

# Security settings
CORS_ORIGINS=*
BCRYPT_LOG_ROUNDS=12

# Logging settings
LOG_LEVEL=INFO
LOG_FILE=quantumvest.log

# Feature flags (set to 'true' or 'false')
ENABLE_PORTFOLIO_OPTIMIZATION=true
ENABLE_REAL_TIME_DATA=true
ENABLE_BLOCKCHAIN_FEATURES=false
ENABLE_ADVANCED_ANALYTICS=true

# ----------------------------------------------------------------
# 2. Frontend Configuration (Used by web-frontend)
# ----------------------------------------------------------------

# The frontend uses a webpack setup and does not appear to use VITE_ env variables.
# The API endpoint is likely configured in the frontend source code (e.g., in axios setup).
# If you need to change the API URL, you will likely need to modify the frontend source code.
# The default backend port is 5000 in the config.py (DevelopmentConfig) or 8000 (ProductionConfig).
# Assuming the API runs on port 5000 for local development.
# FRONTEND_API_URL=http://localhost:5000
