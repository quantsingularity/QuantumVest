## Running Tests

To run the comprehensive tests for the web-frontend, execute the following command in the `code/web-frontend` directory:

```bash
npm test
```

This will run all tests using Jest and React Testing Library. 