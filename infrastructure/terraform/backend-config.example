# Terraform Backend Configuration Example
# S3 Backend for Remote State Storage

# For production use, create a file named `backend.hcl` with these values:
# bucket         = "quantumvest-terraform-state"
# key            = "prod/terraform.tfstate"
# region         = "us-west-2"
# encrypt        = true
# dynamodb_table = "quantumvest-terraform-locks"
# kms_key_id     = "arn:aws:kms:us-west-2:ACCOUNT_ID:key/KEY_ID"

# Initialize with backend:
# terraform init -backend-config=backend.hcl

# For local development (no remote state):
# terraform init -backend=false

# Setup Instructions:
# 1. Create S3 bucket for state storage:
#    aws s3 mb s3://quantumvest-terraform-state --region us-west-2
#    
# 2. Enable versioning:
#    aws s3api put-bucket-versioning --bucket quantumvest-terraform-state \
#      --versioning-configuration Status=Enabled
#    
# 3. Create DynamoDB table for state locking:
#    aws dynamodb create-table \
#      --table-name quantumvest-terraform-locks \
#      --attribute-definitions AttributeName=LockID,AttributeType=S \
#      --key-schema AttributeName=LockID,KeyType=HASH \
#      --billing-mode PAY_PER_REQUEST \
#      --region us-west-2
#
# 4. Create backend.hcl file with your values (DO NOT commit to git)
# 5. Initialize: terraform init -backend-config=backend.hcl
